# Python Labs

## Overview

In these labs, I built a strong foundation in Python programming by exploring the language's core data types and string manipulation. I worked hands-on with numeric and string data types, learning how to write scripts that perform calculations, handle user input, and format meaningful output. These labs established my understanding of how Python represents and manipulates data—essential knowledge for any developer.

## Labs I Completed

| Lab | Description |
|-----|-------------|
| **Working with Numeric Data Types** | I explored Python's `int`, `float`, `complex`, and `bool` data types, performed arithmetic operations in the interactive shell, and wrote scripts to demonstrate how each type stores and represents different kinds of numbers. |
| **Working with the String Data Type** | I mastered string concatenation, collected user input with the `input()` function, and formatted output using the `.format()` method to create an interactive survey program. |

## Services/Tools I Worked With

- **AWS Cloud9 IDE** — Cloud-based integrated development environment that provided terminal access, file editor, and Python runtime for writing and executing scripts
- **Python 3** — Open-source programming language used for all scripts; leveraged built-in functions and data types to solve problems
- **Python Built-in Functions** — `print()`, `type()`, `str()`, `input()` to manipulate data, inspect types, and interact with users
- **Terminal** — Executed Python scripts from the command line using `python3` and the interactive shell

## Key Tasks I Performed

- Executed Python commands interactively in the Python shell and wrote multi-line scripts in files
- Performed arithmetic operations (addition, subtraction, multiplication, division) and understood how Python handles different numeric types
- Declared variables and assigned different data types (`int`, `float`, `complex`, `bool`) to understand type behavior
- Used the `type()` function to inspect the data type of variables and the `str()` function to convert values for string formatting
- Concatenated strings using the `+` operator and used the `.format()` method with placeholders to create formatted output
- Collected user input with the `input()` function and displayed results back to the user in interactive programs

## What I Demonstrated

- ✅ Proficiency with Python's numeric type system and understanding of when to use `int`, `float`, `complex`, and `bool`
- ✅ Ability to write and execute Python scripts in the AWS Cloud9 IDE
- ✅ Strong grasp of string operations, including concatenation and formatting
- ✅ Understanding of how to collect, process, and display user input
- ✅ Familiarity with Python's built-in functions and the standard library
- ✅ Capability to write interactive, user-friendly programs that communicate effectively with input and output
